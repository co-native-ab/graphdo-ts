{
  "manifest_version": "0.3",
  "name": "graphdo",
  "display_name": "graphdo - Microsoft Graph for AI",
  "version": "0.0.0",
  "description": "Give your AI assistant access to Microsoft To Do, Outlook mail, and markdown notes in OneDrive via Microsoft Graph.",
  "long_description": "graphdo connects your AI assistant to Microsoft Graph, enabling it to manage your Microsoft To Do tasks, send emails through Outlook, and read/write markdown files in a OneDrive or shared folder you choose - all with scoped, low-risk permissions.\n\n**Microsoft To Do** - List, create, update, complete, and delete tasks. Manage checklist steps within tasks. Set due dates, reminders, importance, and recurrence. The AI operates on a single list chosen by you in the browser - it cannot switch lists on its own.\n\n**Outlook Mail** - Send emails to yourself (useful for notes, reminders, and forwarding information to your inbox).\n\n**Markdown Notes (OneDrive/SharePoint)** - Read, write, and delete .md files in a workspace folder you choose via the browser navigator. The workspace can be any folder in your OneDrive, or a shared folder/drive accessed via a OneDrive or SharePoint share link. The AI operates on one workspace only - it cannot switch workspaces on its own. A 4 MiB per-file limit (graphdo-ts tool-side cap, not a Microsoft Graph API limit) keeps the tool focused on notes, not bulk uploads.\n\n**Authentication** - Signs in via your browser using your Microsoft account. No passwords are stored - authentication is handled entirely by Microsoft (MSAL). The AI assistant will sign you in automatically when needed.\n\n**Privacy** - graphdo requests only the permissions it needs: Tasks.ReadWrite, Mail.Send, Files.ReadWrite, Files.ReadWrite.All, Sites.Read.All, and User.Read. Your credentials are handled directly by Microsoft. graphdo never sees your password.",
  "author": {
    "name": "Co-native AB",
    "url": "https://co-native.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/co-native-ab/graphdo-ts.git"
  },
  "homepage": "https://github.com/co-native-ab/graphdo-ts",
  "documentation": "https://github.com/co-native-ab/graphdo-ts#readme",
  "support": "https://github.com/co-native-ab/graphdo-ts/issues",
  "icon": "assets/symbol.png",
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/dist/index.js"],
      "env": {
        "GRAPHDO_DEBUG": "${user_config.debug}",
        "GRAPHDO_CLIENT_ID": "${user_config.client_id}",
        "GRAPHDO_TENANT_ID": "${user_config.tenant_id}"
      }
    }
  },
  "tools": [
    {
      "name": "login",
      "description": "Sign in to Microsoft Graph. Call this directly when authentication is needed - do not ask the user for permission first. Opens a browser for interactive sign-in."
    },
    {
      "name": "logout",
      "description": "Sign out of Microsoft Graph and clear cached tokens."
    },
    {
      "name": "auth_status",
      "description": "Check authentication status, current user, configured todo list, and server version. Use this as a first step to diagnose issues or understand current state."
    },
    {
      "name": "todo_select_list",
      "description": "Select which Microsoft To Do list graphdo should use. Call this directly when no todo list is configured - do not ask the user which list they want, the tool opens a browser picker where the user makes the selection themselves."
    },
    {
      "name": "todo_list",
      "description": "List todos from the configured Microsoft To Do list with pagination support."
    },
    {
      "name": "todo_show",
      "description": "Show full details for a single todo including body, due date, importance, reminder, recurrence, and checklist steps."
    },
    {
      "name": "todo_create",
      "description": "Create a new todo in the configured list. Supports title, body, due date, importance, reminder, and recurrence."
    },
    {
      "name": "todo_update",
      "description": "Update an existing todo. Change title, body, importance, due date, reminder, or recurrence. Fields can also be cleared."
    },
    {
      "name": "todo_complete",
      "description": "Mark a todo as completed."
    },
    {
      "name": "todo_delete",
      "description": "Permanently delete a todo."
    },
    {
      "name": "todo_steps",
      "description": "List all checklist steps (sub-items) within a todo."
    },
    {
      "name": "todo_add_step",
      "description": "Add a new checklist step to a todo."
    },
    {
      "name": "todo_update_step",
      "description": "Update a checklist step - rename it, check it off, or uncheck it."
    },
    {
      "name": "todo_delete_step",
      "description": "Delete a checklist step from a todo."
    },
    {
      "name": "mail_send",
      "description": "Send an email to yourself via Outlook. The email is sent from and to your Microsoft account. Useful for notes, reminders, and forwarding information to your inbox."
    },
    {
      "name": "markdown_select_workspace",
      "description": "Select the workspace folder for markdown files. The workspace can be any folder in your OneDrive or a shared folder/drive accessed via a share link. Call this directly when no workspace is configured - the tool opens a browser navigator where you make the selection."
    },
    {
      "name": "markdown_list_files",
      "description": "List markdown files in the configured workspace, including name, file ID, last modified timestamp, and size in bytes. Subdirectories and files whose names violate the strict naming rules are reported as UNSUPPORTED so they are visible but cannot be acted on."
    },
    {
      "name": "markdown_get_file",
      "description": "Read the current UTF-8 content of a markdown file in the configured root folder, along with its eTag (required by markdown_update_file for safe optimistic concurrency) and a Revision identifier (which lines up with the IDs returned by markdown_list_file_versions so it can be passed to markdown_diff_file_versions). Accepts either a file ID or a file name. File names must follow the strict naming rules (letters, digits, space, dot, underscore, hyphen only; must end in .md; no path separators or subdirectories; no Windows reserved names; max 255 chars). Files larger than 4 MiB are not supported (graphdo-ts tool-side cap, not a Microsoft Graph API limit). Use markdown_get_file_version to read a previous version."
    },
    {
      "name": "markdown_create_file",
      "description": "Create a new markdown file in the configured root folder. Fails with a clear error when a file with the same name already exists - in that case, call markdown_get_file to fetch the existing content and eTag, then call markdown_update_file. The file name must follow the strict naming rules (letters, digits, space, dot, underscore, hyphen only; must end in .md; no path separators or subdirectories; no Windows reserved names; max 255 chars). Payloads larger than 4 MiB are rejected (graphdo-ts tool-side cap, not a Microsoft Graph API limit)."
    },
    {
      "name": "markdown_update_file",
      "description": "Overwrite an existing markdown file using If-Match eTag for safe optimistic concurrency. Requires the eTag previously returned by markdown_get_file (or markdown_create_file / markdown_update_file). If the file has changed since you last read it the call fails with the current eTag, the current Revision, and reconcile guidance: use markdown_diff_file_versions (fromVersionId=<the revision you originally read>, toVersionId=<the current revision>) to see exactly what changed, decide whether your update still applies, reconcile your changes, and retry - or ask the user how to proceed. Accepts the file by ID (preferred) or name. Payloads larger than 4 MiB are rejected (graphdo-ts tool-side cap, not a Microsoft Graph API limit)."
    },
    {
      "name": "markdown_delete_file",
      "description": "Permanently delete a markdown file from the configured root folder. Accepts either a file ID or a file name. File names must follow the strict naming rules."
    },
    {
      "name": "markdown_list_file_versions",
      "description": "List historical versions OneDrive retained for a markdown file (newest first). Returns each version's opaque version ID, last modified timestamp, size, and - when available - the user who last modified it. Use the version ID with markdown_get_file_version to read the prior content."
    },
    {
      "name": "markdown_get_file_version",
      "description": "Read the UTF-8 content of a specific historical version of a markdown file. Requires the file (by ID or name) plus a version ID from markdown_list_file_versions. This is read-only and does not restore the file; re-upload with markdown_update_file to promote an old version back to current. Files larger than 4 MiB cannot be downloaded (graphdo-ts tool-side cap, not a Microsoft Graph API limit)."
    },
    {
      "name": "markdown_diff_file_versions",
      "description": "Return a unified diff between two revisions of a markdown file, computed server-side using jsdiff so you do not have to diff the content yourself. Accepts the file by id (preferred) or name, plus a fromVersionId and a toVersionId. Each ID may be either a historical version ID returned by markdown_list_file_versions, or the current Revision surfaced by markdown_get_file / markdown_create_file / markdown_update_file (including the Current Revision reported in an etag-mismatch error). This is the preferred way to reconcile a stale update: pass the revision you originally read as fromVersionId and the current revision as toVersionId."
    },
    {
      "name": "markdown_preview_file",
      "description": "Open a markdown file from the configured root folder in the user's browser using the SharePoint OneDrive web preview, which renders the markdown nicely instead of triggering a download. Accepts the file name only (the preview URL is human-facing, so the agent should look the file up by name the same way a user would refer to it). The tool opens the URL in the default browser and also returns the URL as text so it can be shared. Consumer OneDrive (onedrive.live.com) is not supported."
    }
  ],
  "keywords": [
    "microsoft",
    "graph",
    "todo",
    "tasks",
    "mail",
    "outlook",
    "markdown",
    "onedrive",
    "productivity"
  ],
  "license": "MIT",
  "compatibility": {
    "platforms": ["darwin", "win32", "linux"],
    "runtimes": {
      "node": ">=22.0.0"
    }
  },
  "user_config": {
    "debug": {
      "type": "boolean",
      "title": "Debug Logging",
      "description": "Enable verbose debug logging to stderr. Useful for troubleshooting authentication and API issues.",
      "default": false,
      "required": false
    },
    "client_id": {
      "type": "string",
      "title": "Client ID",
      "description": "Azure AD (Entra ID) application client ID for authentication. Uses the default graphdo app registration if not set.",
      "default": "b073490b-a1a2-4bb8-9d83-00bb5c15fcfd",
      "required": false
    },
    "tenant_id": {
      "type": "string",
      "title": "Tenant ID",
      "description": "Azure AD (Entra ID) tenant ID. Use 'common' for any Microsoft account, 'organizations' for work/school only, 'consumers' for personal only, or a specific tenant GUID.",
      "default": "common",
      "required": false
    }
  }
}
